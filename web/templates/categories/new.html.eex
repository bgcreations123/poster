<h2>New category</h2>

<%= render "form.html",
           categories: @categories,
           changeset: @changeset,
           action: categories_path(@conn, :create) %>

<%= link "Back", to: categories_path(@conn, :index) %>
